{
  "name": "photo-lambda",
  "version": "1.0.0",
  "private": true,
  "description": "Lambda SQS handler for image processing",
  "scripts": {
    "build": "tsc",
    "test": "cd .. && npx vitest run lambda/__tests__/handler.test.ts"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.987.0",
    "@aws-sdk/client-s3": "^3.985.0",
    "@aws-sdk/lib-dynamodb": "^3.987.0",
    "exif-reader": "^2.0.3",
    "sharp": "^0.34.5"
  }
}
